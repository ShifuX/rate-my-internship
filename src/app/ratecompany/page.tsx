import { RateCard } from "../_components";

const page = () => {
  return (
    <div>
      <form className="w-full flex content-center justify-center pt-44">
        <RateCard />
      </form>
    </div>
  );
};

export default page;
